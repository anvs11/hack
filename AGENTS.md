# AGENTS.md

## Назначение

Этот файл обязателен для любого человека или ИИ-агента, работающего в репозитории.
Сначала прочитайте `README.md`, затем `rules.md` и `contracts/openapi.yaml`.

## Постоянный контекст проекта

Следующие файлы образуют единый защищённый контекст проекта:

- `AGENTS.md` — процесс работы агентов и владельцы зон;
- `README.md` — назначение, структура и канонические команды запуска;
- `rules.md` — архитектурные инварианты и порядок изменения контекста;
- `CONTEXT_PACK.md` — продуктовый контекст, факты и проверяемые гипотезы;
- `contracts/openapi.yaml` — API и модели обмена;
- `contracts/examples/*.json` — эталонные payload-примеры;
- `data/seed/*.json` — воспроизводимый demo-набор.

Нельзя менять один из этих файлов изолированно, если изменение затрагивает
архитектуру, API, схему данных, seed или команды запуска. В том же коммите агент
обязан обновить все затронутые файлы постоянного контекста, включая
`CONTEXT_PACK.md`, если изменились продуктовые границы или архитектура. В отчёте перечислить,
что синхронизировано. Если контекст менять не потребовалось — написать это явно.

`README.md`, `rules.md` и `contracts/` меняются только по явному поручению владельца
проекта и одним синхронизированным набором изменений. До такого поручения действующий
контракт остаётся источником истины.

## Архитектурные владельцы

### Инженер 1 — весь backend

Владеет:

- `backend/app/main.py`, конфигурацией БД и Python-зависимостями;
- `backend/app/modules/sources/`;
- `backend/app/modules/publications/`;
- `backend/app/modules/analysis/`;
- `backend/app/modules/prioritization/`;
- `backend/app/modules/evaluation/`;
- `backend/app/modules/decisions/`;
- `backend/app/modules/regulatory_cases/`;
- `backend/app/modules/digests/`;
- `data/seed/`, `data/eval/`, `scripts/seed_demo.py`.

### Инженер 2 — frontend и demo-интеграция

Владеет:

- `frontend/`;
- `tests/e2e/`;
- `docs/DEMO_SCENARIO.md`.

### Общая зона

- `contracts/`, `README.md`, `rules.md`, `AGENTS.md`;
- изменения общей зоны требуют явного поручения владельца проекта и синхронизации
  backend, frontend, OpenAPI, примеров и документации;
- frontend генерирует типы из OpenAPI; ручные aliases, API-клиент и mock fixtures
  должны использовать те же имена полей и схем.

## Канонический HTTP naming v0.4.0

- Пути состоят из существительных во множественном числе.
- Создание вложенного ресурса: `POST /api/<resources>/{id}/<subresources>`.
- Идемпотентная связь двух известных ресурсов: `PUT` с обоими ID в пути.
- В URL не используются action-глаголы `analyze`, `refresh`, `run` и `import`.
- Синхронный сбор возвращает фактический `CollectionReport`; нельзя возвращать
  фиктивный `job_id`, если нет endpoint статуса и хранилища заданий.
- Канонические схемы обмена: `Publication`, `AnalysisVersion`,
  `SpecialistDecision`, `DuplicateCandidate`, `DuplicateReview`, `RegulatoryCase`,
  `LifecycleEvent`.

## Жёсткие инварианты

1. AI-анализ и решение специалиста хранятся раздельно и версионируются.
2. Новая версия не перезаписывает предыдущую.
3. Финальный приоритет назначает только специалист; AI даёт предложение.
4. Важность детерминирована: LLM предлагает именованные критерии, обычный код считает
   `importance_score`; неизвестное значение не превращается в ноль.
5. События lifecycle НПА append-only; неверные переходы отклоняются.
6. СМИ и Telegram не являются официальным подтверждением стадии НПА.
7. Seed работает без сети, повторный импорт не создаёт дубли.
8. SQLite-файлы, секреты и `.env` не коммитятся.
9. Alembic в demo-MVP не используется: чистая SQLite создаётся через `create_all`.
10. Нельзя объединять ветку при падающем unit/E2E-тесте её модуля.
11. Исправления публикации и решения по semantic duplicates append-only; исходный
    материал и предыдущие вердикты не перезаписываются.
12. Telegram Mini App доверяет только `initData`, подпись и срок которого проверил
    backend с bot token; `initDataUnsafe` не является аутентификацией.
13. Semantic similarity сортирует очередь кандидатов, но без независимой разметки не
    удаляет и не объединяет публикации автоматически.
14. Кэш embeddings валиден только для той же публикации, модели и `content_hash`;
    частичный backfill обязан быть продолжимым без потери уже рассчитанных пакетов.
15. Live LLM summary принимается только как 3-5 предложений и для исходника от 500
    символов должно быть короче исходного текста; replay оценивается отдельно.
16. В collection report `already_seen` означает повторный polling по ID/URL,
    `content_duplicates` — совпадение SHA-256 текста; это не semantic verdict.

## Процесс работы

1. Изучить постоянный контекст и код своей зоны до изменений.
2. Проверить, не меняет ли задача архитектуру, API, seed или команды запуска.
3. Сделать минимальный работающий дифф без рефакторинга вне задачи.
4. Выполнить проверки из README и проверки изменённого модуля.
5. Проверить `git diff` и `git status`; не добавлять временные файлы.
6. В отчёте указать сделанное, проверки и синхронизацию постоянного контекста.

## Инженерные принципы

- Честность: не выдавать желаемое за сделанное; неизвестное обозначать явно.
- KISS/YAGNI: использовать существующий код, стандартную библиотеку и уже
  установленные зависимости; не создавать абстракции «на будущее».
- Причина важнее симптома: перед исправлением проследить поток данных до корня.
- Код должен быть легко заменить или удалить.
- Валидировать входные данные на границах доверия и не терять данные при
  преобразованиях.
- Временные артефакты хранить в `/tmp`, проектные — только в репозитории.
- Никогда не выполнять массовый `git add`; добавлять файлы точечно.
- Не пушить и не сливать изменения без явного поручения владельца.

## Ветки и интеграция

- `main` — интеграционная ветка;
- `feat/content-pipeline` — контур инженера 1;
- `feat/analyst-workflow` — контур инженера 2;
- короткие PR предпочтительнее больших: один логический результат на PR;
- `README.md` обновляется в интеграционном PR, кроме отдельного согласованного
  изменения постоянного контекста.
