# RegRadar: воспроизводимый E2E и demo-сценарий

## Назначение

Demo проверяет RegRadar через настоящий локальный FastAPI и отдельную временную
SQLite: фильтры → новости → анализ → выбранные снимки → локальный отчёт → экспорт.
Сохранён отдельный B8-сценарий решения специалиста, привязки к НПА, официального
lifecycle event и автоматической сводки. Продуктовые изменения выполняются через
UI; напрямую запускаются только существующий seed и два локальных процесса.
Backend, seed, контракт и рабочая БД для редизайна не изменялись.

## Предварительные требования

- Python virtual environment `.venv` с зависимостями из
  `backend/requirements-dev.txt`;
- Node.js и зависимости `frontend/node_modules` из lock-файла;
- Chromium, установленный для текущей версии Playwright;
- свободные локальные порты `127.0.0.1:8000` и `127.0.0.1:5173`.

Первичная установка может требовать интернет:

```bash
python3 -m venv .venv
.venv/bin/python -m pip install -r backend/requirements-dev.txt
npm --prefix frontend ci
cd frontend && npx playwright install chromium
```

После установки Python/npm-зависимостей и Chromium основной E2E не использует
внешнюю сеть, не скачивает модели и не вызывает LLM.

## Однокомандный E2E

Из корня репозитория:

```bash
npm --prefix frontend run e2e
```

Для локальной диагностики с видимым Chromium:

```bash
npm --prefix frontend run e2e:headed
```

Runner проверяет, что оба порта свободны, создаёт уникальный каталог через
`mkdtemp`, запускает существующий `scripts/seed_demo.py`, передаёт временную SQLite
в backend через `HACK_DATABASE_URL`, запускает frontend с
`VITE_API_BASE_URL=http://127.0.0.1:8000`, ожидает health/UI и только затем запускает
Playwright. В `finally` он завершает обе группы дочерних процессов и удаляет весь
временный каталог вместе с БД и диагностическими trace/screenshots Playwright.
Сценарии RegRadar отдельно сохраняют демонстрационные PNG и печатный PDF в
`/tmp/regradar-screenshots`; эти артефакты остаются после теста и не коммитятся. `workers=1`; существующие
серверы намеренно не переиспользуются.

## Ручной запуск с real API

В терминале из корня репозитория создайте изолированную БД и сохраните путь в
переменных текущей shell-сессии:

```bash
DEMO_TMP_DIR="$(mktemp -d "${TMPDIR:-/tmp}/hack-demo.XXXXXX")"
DEMO_DB="$DEMO_TMP_DIR/demo.sqlite3"
.venv/bin/python scripts/seed_demo.py --db "$DEMO_DB"
export HACK_DATABASE_URL="sqlite:///$DEMO_DB"
```

Запустите backend и запомните PID:

```bash
.venv/bin/python -m uvicorn backend.app.main:app --host 127.0.0.1 --port 8000 &
DEMO_BACKEND_PID=$!
```

Запустите frontend в той же shell-сессии либо повторно экспортируйте переменные в
новой:

```bash
VITE_API_BASE_URL=http://127.0.0.1:8000 npm --prefix frontend run dev -- --host 127.0.0.1 --port 5173 --strictPort &
DEMO_FRONTEND_PID=$!
```

Проверки готовности и URL интерфейса:

```bash
curl --fail http://127.0.0.1:8000/api/health
curl --fail http://127.0.0.1:5173/feed
```

Откройте `http://127.0.0.1:5173/feed`.

## Начальное ожидаемое состояние

Чистый seed содержит:

- 5 источников;
- 10 публикаций;
- 10 результатов replay-анализа;
- один `case-001` с номером `DEMO-2026-001` и стадией `draft` («Проект»);
- 0 `SpecialistDecision`;
- 0 `LifecycleEvent`;
- ни одной связи публикации с `case-001`.

## Основной путь RegRadar

1. Откройте `/feed`: «Мониторинг», активные новости за всё время, поиск и фильтры.
   В demo не установлен узкий период, скрывающий seed.
2. Выберите категорию или AI-приоритет. Проверьте total и активный фильтр; удалите
   его отдельной кнопкой. Раскройте «Все фильтры»: источник, тип, флаг AI, видимость.
   «AI не запрашивает проверку» не означает человеческого подтверждения.
3. Для проверки пагинации выберите 5 на странице, перейдите далее. Откройте анализ
   нижней карточки и вернитесь к результатам: URL и позиция восстанавливаются.
   Back/Forward браузера восстанавливают применённую выдачу.
4. В анализе прочитайте исходный текст, саммари, факты, сущности, критерии и цитаты.
   Решение специалиста показано отдельно. Без анализа виден обозначенный фрагмент
   исходника, неизвестные значения не превращаются в ноль.
5. Нажмите «В отчёт». Вернитесь в мониторинг, отметьте ещё несколько чекбоксов и
   нажмите пакетное «В отчёт». Повторный пакет с тем же ID не увеличивает счётчик.
   Кнопка «В отчёте» удаляет материал; она не открывает его анализ.
6. Откройте «Отчёт» → «Мой отчёт». Задайте название, резюме, комментарий для
   менеджера. Кнопками ↑/↓ измените порядок. Перезагрузите страницу: всё сохраняется.
7. Скачайте Markdown и JSON. В файлах только выбранные снимки в нужном порядке,
   комментарии, источники, версии и статусы специалиста. Непроверенные материалы
   допустимы, но не превращаются в подтверждённые.
8. Откройте «Просмотр» или «Печатный вид / PDF», затем «Печать / сохранить PDF».
   В печати — белый отчёт RegRadar без навигации, карты и элементов управления.
   Сохранение PDF выполняет браузер; отправка руководителю не реализована.
9. Для проверки версий откройте из отчёта материал с решением по текущему AI и
   явно запустите «Повторить AI-анализ». Старое решение отмечено как относящееся
   к предыдущей версии. При возврате отчёт предлагает «Обновить снимок»;
   до нажатия экспорт использует сохранённый анализ. После обновления комментарий
   для менеджера остаётся прежним. Этот шаг изменяет временную demo-БД, поэтому
   B8-счётчики ниже проверяются отдельно на чистом seed.
10. На desktop сфокусируйте узел карты и нажмите Enter: откроется соответствующая
    публикация. Hover/focus подсвечивает карточку; «Показать карточку в ленте»
    прокручивает к ней. Рёбра отражают общие теги/сущности и отключаемый слой общего источника.
    Основание каждой связи можно прочитать в инспекторе. До 1100px карта
    свёрнута и загружается только после раскрытия.
    Разверните карту на весь экран: потяните для вращения, используйте колесо
    или щипок для масштаба; Shift/два пальца — для перемещения. Сброс возвращает
    исходный вид. Проверьте Escape и возврат фокуса. На мобильном закрытие
    находится в верхнем безопасном отступе; навигация приложения — внизу.
11. «Автоматическая сводка» остаётся независимой: её содержание не добавляется
    в ручной отчёт. Источники, похожие публикации и операции НПА доступны в навигации.

### Локальное хранение и его ограничения

Подпись «Черновик сохранён в этом браузере» описывает localStorage, а не сервер.
Ключ разделён по API-окружению/MSW и подтверждённому Telegram ID. Обычный браузер
имеет один анонимный профиль — без общего backend-входа людей внутри одного
профиля отличить нельзя. На другом устройстве подборка не появляется; одновременное
редактирование одного черновика в нескольких вкладках не имеет разрешения конфликтов.

При ошибке storage изменения остаются в памяти с предупреждением. Повреждённый
оригинал не перезаписывается; в отчёте можно скачать его резервную копию. Недоступный
на сервере материал остаётся в подборке со снимком. Проверка новых версий выполняет
ограниченную очередь существующих GET, не загружая всю базу.

Предложения для следующего этапа описаны в
[REGRADAR_BACKEND_REQUIREMENTS.md](REGRADAR_BACKEND_REQUIREMENTS.md).

## Проверка инвариантов B8 на чистом seed

Этот сценарий выполняйте на чистом seed до ручного создания новых AI-версий и
решений; иначе итоговые счётчики истории закономерно будут отличаться.

1. На `/feed` дождитесь заголовка «Мониторинг» и счётчика 10 материалов.
2. В поле «AI-приоритет» выберите «Высокий». URL должен содержать
   `proposed_priority=high`, а индикатор — «Активно: 1». Публикация «Проект
   требований к обработке данных вынесен на обсуждение» видна; low-публикация
   «ИТ-компания объявила о запуске образовательной программы» отсутствует.
3. Откройте публикацию по заголовку в карточке. Видны «AI-приоритет · Высокий»
   и версия анализа. `analysis-001` доступен в «Версия и технические сведения»;
   раскройте «История анализа и решений» для счётчика «1 AI · 0 решений».
4. В блоке «Подтвердить или скорректировать» не меняйте AI-саммари и категорию.
   В поле «Финальный приоритет» выберите «Критический», в «Комментарий ·
   необязательно» введите `B8 E2E: критический приоритет подтверждён` и нажмите
   «Сохранить решение».
5. Ожидайте сообщение «Решение специалиста сохранено в истории.», статус
   «Скорректировано», финальный приоритет «Критический», комментарий и счётчик
   «1 AI · 1 решений». Исходный AI-приоритет остаётся «Высокий».
6. Нажмите «Привязать к НПА». В диалоге выберите «Демонстрационные требования к
   обработке данных» / `DEMO-2026-001`, затем «Подтвердить привязку». Ожидайте
   «Публикация успешно привязана к НПА.» и метку «Уже привязана». Закройте диалог.
7. В основной навигации нажмите «Кейсы НПА», затем выберите
   «Демонстрационные требования к обработке данных». В `case-001` видны заголовок кейса,
   текущая стадия «Проект», связанная `pub-001` и пустое состояние «Событий пока
   нет».
8. В форме «Добавить официальное событие» задайте:
   - «Стадия»: «Проект»;
   - «Дата события»: `04.09.2026 12:00` (значение input — `2026-09-04T12:00`);
   - «Ссылка на официальное подтверждение»:
     `https://regulation.gov.ru/e2e/case-001`;
   - «Тип официального источника»: «Официальный сайт регулятора»;
   - «Комментарий · необязательно»:
     `B8 E2E: начальная стадия подтверждена`;
   - «Автор события»: `user-gr-001`.
9. Нажмите «Добавить событие». Ожидайте сообщение «Официальное событие добавлено.
   Стадия и хронология обновлены с сервера.». В «Хронологии» ровно одно событие со
   стадией «Проект», датой 4 сентября 2026 12:00, введёнными URL, комментарием и
   автором. Серверная текущая стадия остаётся «Проект».
10. В основной навигации нажмите «Отчёт», затем вкладку «Автоматическая сводка»
    (`/digest?tab=auto`). Дождитесь scope
    `all_available_data` и четырёх разделов: «Подтверждённые критические
    материалы», «Изменения стадий НПА», «Требующие проверки карточки», «Действия
    пользователей».
11. Сводные счётчики должны быть: «Критические» — 1, «Стадии НПА» — 1, «На
    проверке» — 6, «Действия» — 2. Критический раздел содержит `pub-001`,
    «Скорректировано» и «Критический»; lifecycle-раздел — `case-001`, начальную
    стадию «Проект» и lifecycle-комментарий; действия — оба введённых комментария.

## Состояние после сценария

- одна append-only `SpecialistDecision` для `pub-001`: `status=corrected`,
  `final_priority=critical`, анализ `analysis-001` не изменён;
- `pub-001` связана с существующим `case-001`;
- одно начальное append-only `LifecycleEvent` стадии `draft`;
- текущая стадия `case-001` по-прежнему `draft`;
- digest counters: `1 / 1 / 6 / 2` в порядке critical / lifecycle / review / actions.

## Команды проверок

Проверки frontend из корня репозитория:

```bash
npm --prefix frontend run generate:api
git diff --exit-code -- frontend/src/shared/api/schema.d.ts
npm --prefix frontend run typecheck
npm --prefix frontend run lint
npm --prefix frontend run test -- --run
npm --prefix frontend run build
npm --prefix frontend run e2e
git diff --check
git status --short --branch
```

Результат проверки 6 сентября 2026 года: **145 unit-тестов, 8 E2E-сценариев,
typecheck, lint (0 ошибок/предупреждений), production build — прошли**.
Сгенерированный API-файл без diff. E2E проверяет:

1. Прежний B8-путь решения и lifecycle до автоматической сводки.
2. Ленту на 1440×900, 1280×720, 768×1024, 390×844; полную первую карточку на 1280×720.
3. URL, фильтры, включительность последнего дня, пагинацию, Back/Forward и позицию возврата.
4. Отбор без дублей, порядок, комментарии, перезагрузку, новую AI-версию без молчаливого
   изменения снимка, явное обновление, реальные скачивания Markdown/JSON и печатный PDF.
5. Переход из узла с клавиатуры, подсветку карточки, потерю WebGL и 2D-fallback.
6. Анализ, источники, список/деталь НПА, похожие публикации и автоматическую сводку
   на всех четырёх размерах без горизонтального расширения документа.

7. Вращение мышью, масштаб колесом, перемещение, клавиатуру, полноэкранный режим,
   отсутствие перехода по узлу после перетаскивания и обычный переход по клику.
8. Touch rotation/pinch при 390×844, Gilroy, нижнюю навигацию, safe area и
   восстановление свайпов Telegram через SDK harness. Это браузерная эмуляция,
   а не запуск подписанной сессии в нативном Telegram.

Unit-тесты дополнительно покрывают отказ источников отдельно от выдачи, поздний
ответ после отмены, повреждённое/недоступное хранилище, разделение областей хранения,
неизменяемость снимков и статусы решения. Дополнительно проверено сохранение
позиции перед навигацией, если браузер ещё не успел отправить событие scroll. Существующие проверки lifecycle,
дубликатов, Telegram adapter, API-клиента и автоматической сводки сохранены.

Проверить runtime-строки в ручной SQLite можно так:

```bash
sqlite3 "$DEMO_DB" "SELECT id, status, json_extract(payload_json, '$.final_priority') FROM specialist_decisions;"
sqlite3 "$DEMO_DB" "SELECT case_id, publication_id FROM regulatory_case_publications;"
sqlite3 "$DEMO_DB" "SELECT regulatory_case_id, stage, occurred_at, confirmation_source_type FROM lifecycle_events;"
```

Канонический E2E также падает при `console.error`, необработанном `pageerror` и
любом browser request к несуществующему `/api/digest`.

## Остановка и очистка ручного demo

В shell-сессии, где сохранены PID и путь:

```bash
kill "$DEMO_FRONTEND_PID" "$DEMO_BACKEND_PID"
wait "$DEMO_FRONTEND_PID" "$DEMO_BACKEND_PID" 2>/dev/null || true
rm -rf -- "$DEMO_TMP_DIR"
unset HACK_DATABASE_URL DEMO_DB DEMO_TMP_DIR DEMO_FRONTEND_PID DEMO_BACKEND_PID
```

Перед `rm -rf` убедитесь, что `DEMO_TMP_DIR` получен именно командой `mktemp` выше.
Автоматический runner выполняет остановку и удаление самостоятельно даже после
ошибки теста или сигнала завершения.

## Fallback A — real API без сети и LLM

Используйте versioned offline seed и обычные команды ручного запуска выше. Не
запускайте live collection и не выбирайте `live_llm`: все десять анализов получены
replay-анализатором. После предварительной установки зависимостей demo работает без
внешней сети. Replay — заранее сохранённый результат, а не live inference.

## Fallback B — frontend mock

```bash
npm --prefix frontend run dev -- --host 127.0.0.1 --port 5173
```

В dev/test без `VITE_API_BASE_URL` frontend включает MSW. Его fixture-набор содержит
5 публикаций и отличается от десяти публикаций backend seed. Шапка показывает
«Demo · MSW». В production MSW не включается автоматически, запросы идут к API.
Настроенный API отмечается как «Режим API», что не является индикатором health. Этот режим подходит для
демонстрации интерфейса, но не доказывает работу настоящей SQLite, backend
persistence или CORS. Канонический E2E всегда использует real API. Первичная
установка npm/Python dependencies и Playwright Chromium может потребовать сеть.

## Факты текущего demo

- Seed локальный и воспроизводимый.
- Replay не вызывает внешнюю LLM и представляет заранее сохранённый результат.
- `SpecialistDecision` и `LifecycleEvent` append-only.
- Финальный приоритет назначает специалист; AI предлагает приоритет.
- Lifecycle принимает только `regulator` или `official_publication`; Telegram и
  СМИ не подтверждают стадию.
- Дайджест — клиентский снимок по существующим read API.
- Серверного digest endpoint и хранения ручных отчётов нет; версии локальных
  снимков отчёта сохраняет браузер.
- UI выбора существующего НПА реализован.
- UI создания нового НПА отсутствует.

## Гипотезы и то, что demo не доказывает

- качество live LLM и фактическую достоверность AI-саммари;
- юридическую корректность решений специалиста;
- полезность скоринга на реальных данных;
- работоспособность live-сбора всех внешних источников;
- производительность и масштабируемость;
- ценность дайджеста для реальных руководителей;
- email/Telegram-доставку;
- полный аудит действий за пределами `SpecialistDecision` и `LifecycleEvent`.

## Troubleshooting

- **Порт 8000 или 5173 занят.** Остановите найденный процесс и повторите запуск.
  Runner прекращает работу до seed/server startup и не подключается к чужому
  процессу. Диагностика: `lsof -nP -iTCP:8000 -sTCP:LISTEN` и аналогично для 5173.
- **Executable `.venv/bin/python` не найден или нет FastAPI/Uvicorn.** Создайте venv
  и установите `backend/requirements-dev.txt` командами из предварительных
  требований.
- **Chromium executable отсутствует.** Выполните
  `cd frontend && npx playwright install chromium`; загрузка браузера требует сеть
  один раз для установленной версии Playwright.
- **Нет `frontend/node_modules`.** Выполните `npm --prefix frontend ci`.
- **E2E упал.** Консоль Playwright показывает assertion и вывод серверов.
  Диагностические trace/screenshots удаляются вместе с временным каталогом runner;
  демонстрационные артефакты RegRadar остаются в `/tmp/regradar-screenshots`.
  Для интерактивной диагностики используйте `npm --prefix frontend run e2e:headed`.
